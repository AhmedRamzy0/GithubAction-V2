name: first workflow

on:
  push:
    branches:
      - main

jobs:
  example-job:
    runs-on: ubuntu-latest

    steps:
      - name: print a welcome message
        run: echo "welcome Ahmed"

      - name: checkout repository
        uses: actions/checkout@v4.2.2

      - name: list files
        run: ls -la
